describe('ListCustomersUseCase', () => {
  it.todo('returns paginated items with total, page and perPage')
  it.todo('passes search through to the repository')
  it.todo('returns an empty page when there are no customers')
})
